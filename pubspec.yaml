name: flutter_expense_tracker_app
description: A simple cross-platform Flutter expense tracker for Android and iOS with dashboard cards, transaction list, and add expense flow.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
